# ========================================
# DENTAL DATA - SEGUIMIENTO CLIENTES
# ========================================

# === ARCHIVOS SENSIBLES (NUNCA SUBIR) ===
# Credenciales de Google Calendar
credentials.json
client_secret_oauth.json
token.pickle

# Variables de entorno
.env
.env.local
.env.*.local

# === DATOS CON PII (NUNCA SUBIR - compartir por canal seguro) ===
# Seed de clientes con NIF, teléfonos, direcciones
clientes_maestro.json
# Exports derivados de la BD con PII
*.csv

# === PYTHON ===
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environments
venv/
env/
ENV/
.venv

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# === NODE.JS / NEXT.JS ===
# Dependencies
node_modules/
dashboard/node_modules/

# Production build
dashboard/.next/
dashboard/out/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dashboard/npm-debug.log*

# Next.js cache
dashboard/.next/
dashboard/out/

# === DATABASE ===
*.db
*.sqlite
*.sqlite3

# PostgreSQL dumps
*.sql
*.dump

# === ARCHIVOS TEMPORALES DE DESARROLLO ===
# NOTA: NO ignoramos test_*.py — la suite real vive en tests/ y DEBE versionarse.
# (patrón heredado eliminado: antes ignoraba todos los test_*.py)

# Outputs temporales
output*.txt
reporte*.txt
analisis*.json

# Archivos Excel temporales
~$*.xlsx
*.tmp
listado*.xlsx
reuniones*.xlsx
INFORME*.xlsx
INFORME*.md
RESUMEN*.md

# === ARCHIVOS DEL PROYECTO (TEMPORALES) ===
# JSON de prueba
io.json
id_mapping.json
analisis_reuniones_ursula.json

# Excel de desarrollo
clientes_maestro*.xlsx

# Batch scripts temporales
INICIAR_TODO.bat
start_dashboard_only.bat
start_frontend.bat

# Archivos nul
nul

# === LOGS ===
logs/
*.log

# === CLAUDE / AI TOOLING ===
.claude/
.superpowers/
.atl/

# Cloud SQL Auth Proxy binary (se descarga localmente, no se versiona)
cloud-sql-proxy.exe
cloud-sql-proxy

# === SISTEMA OPERATIVO ===
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
*~

# === BACKUPS ===
*.bak
*.backup
*.(old)
*.orig

# === OTROS ===
# Archivos de distribución
dist/
build/
*.egg-info/
